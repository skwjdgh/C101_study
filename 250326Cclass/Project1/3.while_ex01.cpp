/*#define	_CRT_SECURE_NO_WARNINGS
#include <stdio.h>

void main()
{
	/*
	int i = 0;

	while (i < 5)
	{
		printf("while문을 공부합시다. \n");
		i++;
	}
	*/



	/*
	int hap = 0;
	int i;

	i = 0;
	while (i <= 10)
	{
		hap = hap + i;
		i++;
	}

	printf("1에서 10까지의 합 : %d \n", hap);*/


	/*
	int a, b;

	while (1)
	{
		printf("더할 두 수 입력 (멈추려면 Ctrl+C) :");
		scanf("%d %d", &a, &b);

		printf("%d + %d = %d \n", a, b, a + b);
	}
	*/
	/*
	int a, b;
	char ch;

	while (1)
	{
		printf("계산할 두 수를 입력 (멈추려면 Ctrl+C) :");
		scanf("%d %d", &a, &b);

		printf("계산할 연산자를 입력하세요.: ");
		scanf(" %c", &ch);

		switch (ch)
		{
		case '+' :
			printf("%d + %d = %d\n", a, b, a + b);
			break;
		case '-':
			printf("%d - %d = %d\n", a, b, a - b);
			break;
		case '*':
			printf("%d * %d = %d\n", a, b, a * b);
			break;
		case '/':
			printf("%d / %d = %f\n", a, b, a / (float)b);
			break;
		case '%':
			printf("%d %% %d = %d\n", a, b, a % b);
			break;
		default:
			printf("잘못 입력하셨습다.\n");
		}
	}
	*/
//}*/